# ============================================================
# Ecuro MCP Server - Variáveis de Ambiente
# ============================================================

# Token de acesso à API Ecuro Light (obrigatório)
ECURO_ACCESS_TOKEN=seu_token_aqui

# URL base da API (opcional - padrão: produção)
# ECURO_API_BASE_URL=https://clinics.api.dev.ecuro.com.br/api/v1/ecuro-light

# Supabase (obrigatório para buscas de dentistas)
SUPABASE_URL=https://seu-projeto.supabase.co
SUPABASE_KEY=sua_chave_de_servico_aqui

# Transport: "stdio" (padrão) ou "http"
TRANSPORT=stdio

# Porta HTTP (apenas se TRANSPORT=http)
PORT=3000
